Deciding about all the software components that must be used. Until now I have identified:

streamlit for the GUI which I will have to explore deeper, which is a risk because of it costing time
sqlalchemy library for the database and managing data. Also using the sqlalchemy orm will be preffered rather than using raw SQL querries because even though it has a learning curve, eventually it will be more useful in the long run both for personal knowledge and for my career.
Maybe pandas library for manipulating data in memory for function-specific purposes
Docker to package the final app for shipping so it can run on the client's machine. This is also a risk because I have not worker with Docker so I have to explore the technology.
For the function id_generator, I had to decide if I want to have one function that generates both id types (employee and shift) and depending on input it could decide which algorithm to run, or I could have to separate functions where one generates employee id and the other generates shift_id. I decided to go with the second option because it is more flexible and follows the Single Responsibility Principle.
